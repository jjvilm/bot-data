<nav class="navbar navbar-expand-lg navbar-light bg-white shadow">
  <div class="container-fluid">
    <a class="navbar-brand text-uppercase" href="<% if (locals.user && locals.user.role === 'DataEntry') { %>/deDashboard<% } else if (locals.user && locals.user.role === 'QualityControl') { %>/qcDashboard<% } else if (locals.user && locals.user.role === 'Admin') { %>/adminDashboard<% } else { %>/qcDashboard/botList<% } %>">
      <img src="/images/aetherborne.jpg" alt="Aetherborne Logo" style="height: 1.5em; vertical-align: middle; margin-right: 0.5em;">       Aetherborne
    </a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        <% if (locals.user && locals.user.role === 'DataEntry') { %>
        <li class="nav-item">
          <a class="nav-link text-uppercase" aria-current="page" href="/deDashboard">DE-Dashboard</a>
        </li>
        <% } %>
        <% if (locals.user && locals.user.role === 'QualityControl') { %>
        <li class="nav-item">
          <a class="nav-link text-uppercase" aria-current="page" href="/qcDashboard">QC-Dashboard</a>
        </li>
        <% } %>
        <% if (locals.user && locals.user.role === 'Admin') { %>
        <li class="nav-item">
          <a class="nav-link text-uppercase" href="/adminDashboard">Admin Dashboard</a>
        </li>
        <% } %>
        <% if (locals.user) { %>
        <li class="nav-item">
          <a class="nav-link text-uppercase" href="/account/logout">Logout</a>
        </li>
        <% } else { %>
        <li class="nav-item">
          <a class="nav-link text-uppercase" href="/account/login">Log in</a>
        </li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>
