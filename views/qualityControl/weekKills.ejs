<!DOCTYPE html>
<html lang="en">
<%- include('../partials/headfile'); %>
<link rel="stylesheet" href="/stylesheets/botList.css" />

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Bot Hunting Data</title>
    <style>
        .input-group-prepend .input-group-text {
            font-size: 0.8rem;
        }
        .form-control {
            font-size: 0.8rem;
            width: 60px;
        }
        .filter-container {
            display: none; /* Initially hidden */
            position: fixed;
            top: 120px; /* Adjusted to be below the buttons */
            right: 10px; /* Positioned to the right */
            z-index: 1000; /* Ensure it is above other elements */
            background-color: white; /* Add background color */
            padding: 10px; /* Add padding */
            border: 1px solid #ccc; /* Add border */
            border-radius: 5px; /* Add border radius */
        }
        @media (max-width: 768px) {
            .filter-container {
                width: 100%; /* Make it full width on smaller screens */
                right: 0;
                top: 120px; /* Adjust top position */
                padding: 5px; /* Adjust padding */
            }
        }
        .table th, .table td {
            text-align: center; /* Center the contents of the table */
        }
        .hunter-name {
            font-size: 0.6rem; /* Very small font size */
            text-align: right; /* Align to the bottom-right */
            display: block;
            margin-top: 5px;
        }
        .hidden {
            display: none;
        }
    </style>
</head>

<body>
    <%- include('../partials/navbar'); %>

    <!-- Add your buttons here -->
    <div class="d-flex flex-wrap justify-content-center mb-4">
        <div class="btn-group mr-2" role="group" aria-label="Button group">
            <a href="/deRoute/botList" class="btn btn-success" style="font-weight: bold; color: black;">All Bots</a>
            <a href="/deRoute/topWorlds" class="btn btn-secondary" style="font-weight: bold; color: black;">World Kills</a>
            <a href="/qcRoute/commonlyVisitedWorlds" class="btn btn-danger" style="font-weight: bold; color: black;">Top Worlds</a>
            <a href="/deRoute/EquipmentSets" class="btn btn-dark" style="font-weight: bold; color: white;">Equipments</a>
            <!-- Filter button -->
            <button id="toggleFilterBtn" class="btn btn-info btn-sm">Filter</button>
        </div>
    </div>

    <!-- Move input fields to the bottom left corner below the navbar -->
    <div class="filter-container" id="filterContainer">
        <div class="input-group mr-2">
            <div class="input-group-prepend">
                <span class="input-group-text" id="combatLevelLabel" style="background-color: #343a40; color: white;">Combat Level</span>
            </div>
            <input type="number" id="combatLevelInput" class="form-control" placeholder="Enter" min="3" max="126" aria-label="Combat Level" aria-describedby="combatLevelLabel">
        </div>
        <div class="input-group mr-2">
            <div class="input-group-prepend">
                <span class="input-group-text" id="combatRangeLabel" style="background-color: #343a40; color: white;">Range (±)</span>
            </div>
            <input type="number" id="combatRangeInput" class="form-control" placeholder="Enter" min="1" max="55" value="10" aria-label="Combat Range" aria-describedby="combatRangeLabel">
        </div>
        <button id="filterBotsBtn" class="btn btn-primary btn-sm">Filter Bots</button>
    </div>

    <div class="container">
        <h4 style="text-align: center;">Hunted Bots</h4>

        <div class="table-responsive">
            <table class="table table-hover" id="week-kills-table">
                <thead>
                    <tr>
                        <th>Bot</th>
                        <th>World</th>
                        <th>Time</th>
                        <th>Loot</th>
                    </tr>
                </thead>
                <tbody id="botTableBody">
                    <!-- Placeholder for no bots message -->
                </tbody>
            </table>
            <!-- Container for no bots message -->
            <div id="noBotsMessageContainer" style="display: none; text-align: center; font-size: 1.5rem; margin-top: 20px;">
                No Recent Bot Hunts
            </div>
            <!-- Modal -->
            <div class="modal fade" id="updateModal" tabindex="-1" aria-labelledby="updateModalLabel" aria-hidden="true">
                <div class="modal-dialog">
                    <div class="modal-content">
                        <div class="modal-header">
                            <h5 class="modal-title" id="updateModalLabel">Updating: <strong></strong></h5>
                            <button type="button" class="close" data-dismiss="modal" aria-label="Close">
                                <span aria-hidden="true">&times;</span>
                            </button>
                        </div>
                        <div class="modal-body">
                            <form method="post" action="/qcRoute/botUpdate">
                                <input type="hidden" name="id" />
                                <div class="form-group">
                                    <label for="combat_lv">Combat Level:</label>
                                    <input type="number" class="form-control" name="combat_lv" min="0" max="126" id="combat_lv" placeholder="Enter Combat Level" required maxLength="3">
                                    <span id="combatLevelError" style="color:red;"></span>
                                </div>
                                <div class="form-group">
                                    <label for="comments">Comments:</label>
                                    <input type="text" class="form-control" name="comments" id="comments" placeholder="Enter Any Comments" maxLength="80">
                                </div>
                                <button type="submit" class="btn btn-primary">Submit</button>
                            </form>
                        </div>
                    </div>
                </div>
            </div>

            <%- include('../partials/weekKillsScript.ejs'); %>
        </div>
        <!-- Refresh button -->
        <div id="refreshButtonContainer" style="display: none; text-align: center; margin-top: 20px;">
            <button id="refreshPageBtn" class="btn btn-success">Refresh</button>
        </div>
    </div>
</body>
</html>

<script>
    // Toggle filter container visibility
    document.getElementById('toggleFilterBtn').addEventListener('click', function() {
        const filterContainer = document.getElementById('filterContainer');
        filterContainer.style.display = filterContainer.style.display === 'none' ? 'block' : 'none';
    });

    // Hide filter container when clicking outside of it
    document.addEventListener('click', function(event) {
        const filterContainer = document.getElementById('filterContainer');
        const toggleFilterBtn = document.getElementById('toggleFilterBtn');
        if (!filterContainer.contains(event.target) && !toggleFilterBtn.contains(event.target)) {
            filterContainer.style.display = 'none';
        }
    });

    // Filter bots when the filter button is clicked
    document.getElementById('filterBotsBtn').addEventListener('click', function() {
        const combatLevel = document.getElementById('combatLevelInput').value;
        const combatRange = document.getElementById('combatRangeInput').value;
        // Add your filtering logic here
        // Example: filterTable(combatLevel, combatRange);
    });

    // Function to filter the table based on combat level and range
    function filterTable(combatLevel, combatRange) {
        const table = document.getElementById('week-kills-table');
        const rows = table.getElementsByTagName('tr');
        for (let i = 1; i < rows.length; i++) {
            const cells = rows[i].getElementsByTagName('td');
            const botCombatLevel = parseInt(cells[0].innerText);
            if (Math.abs(botCombatLevel - combatLevel) <= combatRange) {
                rows[i].style.display = '';
            } else {
                rows[i].style.display = 'none';
            }
        }
    }

    // Check if there are any bots to display after the DOM is fully loaded
    document.addEventListener('DOMContentLoaded', function() {
        const botTableBody = document.getElementById('botTableBody');
        const noBotsMessageContainer = document.getElementById('noBotsMessageContainer');
        const refreshButtonContainer = document.getElementById('refreshButtonContainer');
        const hasBots = botTableBody.children.length > 0 && botTableBody.children[0].tagName === 'TR' && botTableBody.children[0].children.length > 0;
        if (hasBots) {
            noBotsMessageContainer.style.display = 'none';
            refreshButtonContainer.style.display = 'none';
        } else {
            noBotsMessageContainer.style.display = 'block';
            refreshButtonContainer.style.display = 'block';
        }
    });

    // Refresh the page when the refresh button is clicked
    document.getElementById('refreshPageBtn').addEventListener('click', function() {
        location.reload();
    });
</script>